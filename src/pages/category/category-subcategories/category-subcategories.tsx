import { SubcategoryProps } from "@/types"
import React from 'react'

type Props = {
    slug: SubcategoryProps["slug"]
}

const CategorySubcategories: React.FC<Props> = ({ slug }) => {
    
    
  return (
    <div>
      
    </div>
  )
}

export default CategorySubcategories
